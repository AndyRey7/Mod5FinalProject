import React from 'react'

const Profile = (props) => {
    return (
        <div>
        <h1>Welcome to your profile {props.user.name}</h1>
        <p></p>

        </div>
    )
}

export default Profile
